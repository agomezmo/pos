# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pos_system
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=pos-system-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# Server
PORT=4000
NODE_ENV=development
